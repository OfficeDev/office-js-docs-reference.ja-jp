### YamlMime:UniversalReference
items:
  - uid: excel_release.Excel.RunOptions
    name: Excel.RunOptions
    fullName: Excel.RunOptions
    langs:
      - typeScript
    type: interface
    extends:
      - 'OfficeExtension.RunOptions<Session> '
    package: excel_release
    children:
      - excel_release.Excel.RunOptions.delayForCellEdit
  - uid: excel_release.Excel.RunOptions.delayForCellEdit
    summary: |-
      <span data-ttu-id="09e25-101">ユーザーがセル編集モードを終了するまで、バッチ要求を延期するかどうかを指定します。</span><span class="sxs-lookup"><span data-stu-id="09e25-101">Determines whether Excel will delay the batch request until the user exits cell edit mode.</span></span>

      <span data-ttu-id="09e25-102">false の場合、バッチ要求がホストによって処理されているときに、ユーザーがセルの編集を行っていると、バッチは自動的に失敗します。</span><span class="sxs-lookup"><span data-stu-id="09e25-102">When false, if the user is in cell edit when the batch request is processed by the host, the batch will automatically fail.</span></span> <span data-ttu-id="09e25-103">true の場合、ユーザーがセル編集モードでない場合、バッチ要求はすぐに実行されますが、ユーザーがセル編集モードの場合は、ユーザーがセル編集モードを終了するまで、バッチ要求は遅延されます。</span><span class="sxs-lookup"><span data-stu-id="09e25-103">When true, the batch request will be executed immediately if the user is not in cell edit mode, but if the user is in cell edit mode the batch request will be delayed until the user exits cell edit mode.</span></span>
    name: delayForCellEdit
    fullName: delayForCellEdit
    langs:
      - typeScript
    type: property
    syntax:
      content: 'delayForCellEdit?: boolean;'
      return:
        type:
          - boolean