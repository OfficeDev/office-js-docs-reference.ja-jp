### YamlMime:UniversalReference
items:
  - uid: office.Office.Document
    summary: <span data-ttu-id="694d9-101">アドインから対話操作するドキュメントを表す抽象クラス。</span><span class="sxs-lookup"><span data-stu-id="694d9-101">An abstract class that represents the document the add-in is interacting with.</span></span>
    remarks: '<span data-ttu-id="694d9-102">**Hosts**: Access、Excel、PowerPoint、Project、Word</span><span class="sxs-lookup"><span data-stu-id="694d9-102">**Hosts**: Access, Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.Document.addHandlerAsync
      - office.Office.Document.addHandlerAsync_1
      - office.Office.Document.getActiveViewAsync
      - office.Office.Document.getActiveViewAsync_1
      - office.Office.Document.getFileAsync
      - office.Office.Document.getFileAsync_1
      - office.Office.Document.getFilePropertiesAsync
      - office.Office.Document.getFilePropertiesAsync_1
      - office.Office.Document.getMaxResourceIndexAsync
      - office.Office.Document.getMaxResourceIndexAsync_1
      - office.Office.Document.getMaxTaskIndexAsync
      - office.Office.Document.getMaxTaskIndexAsync_1
      - office.Office.Document.getProjectFieldAsync
      - office.Office.Document.getProjectFieldAsync_1
      - office.Office.Document.getResourceByIndexAsync
      - office.Office.Document.getResourceByIndexAsync_1
      - office.Office.Document.getResourceFieldAsync
      - office.Office.Document.getResourceFieldAsync_1
      - office.Office.Document.getSelectedDataAsync
      - office.Office.Document.getSelectedDataAsync_1
      - office.Office.Document.getSelectedResourceAsync
      - office.Office.Document.getSelectedResourceAsync_1
      - office.Office.Document.getSelectedTaskAsync
      - office.Office.Document.getSelectedTaskAsync_1
      - office.Office.Document.getSelectedViewAsync
      - office.Office.Document.getSelectedViewAsync_1
      - office.Office.Document.getTaskAsync
      - office.Office.Document.getTaskAsync_1
      - office.Office.Document.getTaskByIndexAsync
      - office.Office.Document.getTaskByIndexAsync_1
      - office.Office.Document.getTaskFieldAsync
      - office.Office.Document.getTaskFieldAsync_1
      - office.Office.Document.getWSSUrlAsync
      - office.Office.Document.getWSSUrlAsync_1
      - office.Office.Document.goToByIdAsync
      - office.Office.Document.goToByIdAsync_1
      - office.Office.Document.removeHandlerAsync
      - office.Office.Document.removeHandlerAsync_1
      - office.Office.Document.setResourceFieldAsync
      - office.Office.Document.setResourceFieldAsync_1
      - office.Office.Document.setSelectedDataAsync
      - office.Office.Document.setSelectedDataAsync_1
      - office.Office.Document.setTaskFieldAsync
      - office.Office.Document.setTaskFieldAsync_1
      - office.Office.Document.bindings
      - office.Office.Document.customXmlParts
      - office.Office.Document.mode
      - office.Office.Document.settings
      - office.Office.Document.url
  - uid: office.Office.Document.addHandlerAsync
    summary: <span data-ttu-id="694d9-103">Document オブジェクトイベントのイベントハンドラーを追加します。</span><span class="sxs-lookup"><span data-stu-id="694d9-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-104">**要件セット**: [documentevents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="694d9-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="694d9-105">各イベントハンドラー関数の名前が一意である限り、指定された eventType に対して複数のイベントハンドラーを追加できます。</span><span class="sxs-lookup"><span data-stu-id="694d9-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="694d9-106">Document オブジェクトイベントの場合、eventType パラメーターは、またはと`Office.EventType.Document.SelectionChanged`して指定できます。`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="694d9-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="694d9-107">、またはこの列挙の対応するテキスト値を指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="694d9-108">追加するイベントハンドラー関数。これは、唯一のパラメーターが DocumentSelectionChangedEventArgs 型であることを示し[ます。](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="694d9-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="694d9-109">.</span><span class="sxs-lookup"><span data-stu-id="694d9-109"></span></span> <span data-ttu-id="694d9-110">必須です。</span><span class="sxs-lookup"><span data-stu-id="694d9-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="694d9-111">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-112">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-112">Optional.</span></span> <span data-ttu-id="694d9-113">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-114">.</span><span class="sxs-lookup"><span data-stu-id="694d9-114"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.addHandlerAsync_1
    summary: <span data-ttu-id="694d9-115">Document オブジェクトイベントのイベントハンドラーを追加します。</span><span class="sxs-lookup"><span data-stu-id="694d9-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-116">**要件セット**: [documentevents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="694d9-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="694d9-117">各イベントハンドラー関数の名前が一意である限り、指定された eventType に対して複数のイベントハンドラーを追加できます。</span><span class="sxs-lookup"><span data-stu-id="694d9-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-118">例</span><span class="sxs-lookup"><span data-stu-id="694d9-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="694d9-119">Document オブジェクトイベントの場合、eventType パラメーターは、またはと`Office.EventType.Document.SelectionChanged`して指定できます。`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="694d9-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="694d9-120">、またはこの列挙の対応するテキスト値を指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="694d9-121">追加するイベントハンドラー関数。これは、唯一のパラメーターが DocumentSelectionChangedEventArgs 型であることを示し[ます。](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="694d9-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="694d9-122">.</span><span class="sxs-lookup"><span data-stu-id="694d9-122"></span></span> <span data-ttu-id="694d9-123">必須です。</span><span class="sxs-lookup"><span data-stu-id="694d9-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="694d9-124">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-124">Optional.</span></span> <span data-ttu-id="694d9-125">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-126">.</span><span class="sxs-lookup"><span data-stu-id="694d9-126"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.getActiveViewAsync
    summary: <span data-ttu-id="694d9-127">プレゼンテーションの現在のビューの状態を返します (編集または読み取り)。</span><span class="sxs-lookup"><span data-stu-id="694d9-127">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="694d9-128">**要件セット**: [activeview](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="694d9-128">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="694d9-129">ビューが変更されたときにイベントをトリガーできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-129">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-130">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-130">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-131">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-131">Optional.</span></span> <span data-ttu-id="694d9-132">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-132">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-133">.</span><span class="sxs-lookup"><span data-stu-id="694d9-133"></span></span> <span data-ttu-id="694d9-134">結果`value`のプロパティは、プレゼンテーションの現在のビューの状態です。</span><span class="sxs-lookup"><span data-stu-id="694d9-134">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="694d9-135">返される値は、"edit" または "read" のいずれかです。</span><span class="sxs-lookup"><span data-stu-id="694d9-135">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="694d9-136">"edit" は、スライドを編集できるビュー (標準表示、アウトライン表示など) のいずれかに対応します。</span><span class="sxs-lookup"><span data-stu-id="694d9-136">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="694d9-137">"読み取り" は、スライドショーまたは閲覧表示に対応します。</span><span class="sxs-lookup"><span data-stu-id="694d9-137">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getActiveViewAsync_1
    summary: <span data-ttu-id="694d9-138">プレゼンテーションの現在のビューの状態を返します (編集または読み取り)。</span><span class="sxs-lookup"><span data-stu-id="694d9-138">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="694d9-139">**要件セット**: [activeview](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="694d9-139">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="694d9-140">ビューが変更されたときにイベントをトリガーできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-140">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-141">例</span><span class="sxs-lookup"><span data-stu-id="694d9-141">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-142">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-142">Optional.</span></span> <span data-ttu-id="694d9-143">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-143">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-144">.</span><span class="sxs-lookup"><span data-stu-id="694d9-144"></span></span> <span data-ttu-id="694d9-145">結果`value`のプロパティは、プレゼンテーションの現在のビューの状態です。</span><span class="sxs-lookup"><span data-stu-id="694d9-145">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="694d9-146">返される値は、"edit" または "read" のいずれかです。</span><span class="sxs-lookup"><span data-stu-id="694d9-146">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="694d9-147">"edit" は、スライドを編集できるビュー (標準表示、アウトライン表示など) のいずれかに対応します。</span><span class="sxs-lookup"><span data-stu-id="694d9-147">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="694d9-148">"読み取り" は、スライドショーまたは閲覧表示に対応します。</span><span class="sxs-lookup"><span data-stu-id="694d9-148">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getFileAsync
    summary: '<span data-ttu-id="694d9-149">ドキュメント ファイル全体を、最大で 4194304 バイト (4 MB) のスライスに分割して返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-149">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="694d9-150">iOS 用アドインの場合は、最大 65536 バイト (64 KB) のファイル スライスがサポートされます。</span><span class="sxs-lookup"><span data-stu-id="694d9-150">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="694d9-151">許容された制限を超えてファイル スライスのサイズを指定すると、"内部エラー" が発生する点に注意してください。</span><span class="sxs-lookup"><span data-stu-id="694d9-151">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="694d9-152">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-152">**Requirement sets**:</span></span>

      - <span data-ttu-id="694d9-153">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile)(を使用する場合`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-153">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-154">)</span><span class="sxs-lookup"><span data-stu-id="694d9-154"></span></span>

      - [<span data-ttu-id="694d9-155">ファイル</span><span class="sxs-lookup"><span data-stu-id="694d9-155">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="694d9-156">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile)(を使用する場合`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-156">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="694d9-157">)</span><span class="sxs-lookup"><span data-stu-id="694d9-157"></span></span>

      <span data-ttu-id="694d9-158">office for iOS 以外の office ホストアプリケーションで実行されているアドインの場合、getFileAsync メソッドは最大4194304バイト (4 MB) のスライスでのファイルの取得をサポートしています。</span><span class="sxs-lookup"><span data-stu-id="694d9-158">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="694d9-159">iOS アプリ向けの Office で実行するアドインの場合、getFileAsync メソッドは最大 65536 バイト (64 KB) にスライスしたファイルの取得をサポートします。</span><span class="sxs-lookup"><span data-stu-id="694d9-159">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="694d9-160">filetype パラメーターは、 [Office の filetype](xref:office.Office.FileType)列挙型またはテキスト値を使用して指定できます。</span><span class="sxs-lookup"><span data-stu-id="694d9-160">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="694d9-161">ただし、ホストによって可能な値は次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="694d9-161">But the possible values vary with the host:</span></span>

      <span data-ttu-id="694d9-162">Windows デスクトップ、iPad、excel Online の場合:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-162">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="694d9-163">Excel for Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-163">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-164">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-164"></span></span>

      <span data-ttu-id="694d9-165">powerpoint for Windows デスクトップ、Mac、iPad、powerpoint Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-165">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-166">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-166"></span></span>

      <span data-ttu-id="694d9-167">word for Windows デスクトップ、Mac、iPad、および word Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-167">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-168">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-168"></span></span><!-- --><span data-ttu-id="694d9-169">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-169"></span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-170">例</span><span class="sxs-lookup"><span data-stu-id="694d9-170">Examples</span></span>

      ```javascript
      // The following example gets the document in Office Open XML ("compressed") format in 65536 bytes (64 KB) slices.
      // Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.
      function getDocumentAsCompressed() {
          Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, 
              function (result) {
                  if (result.status == "succeeded") {
                  // If the getFileAsync call succeeded, then
                  // result.value will return a valid File Object.
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);

                  // Get the file slices.
                  getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
                  else {
                  app.showNotification("Error:", result.error.message);
                  }
          });
      }

      function getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {
          file.getSliceAsync(nextSlice, function (sliceResult) {
              if (sliceResult.status == "succeeded") {
                  if (!gotAllSlices) { // Failed to get all slices, no need to continue.
                      return;
                  }

                  // Got one slice, store it in a temporary array.
                  // (Or you can do something else, such as
                  // send it to a third-party server.)
                  docdataSlices[sliceResult.value.index] = sliceResult.value.data;
                  if (++slicesReceived == sliceCount) {
                    // All slices have been received.
                    file.closeAsync();
                    onGotAllSlices(docdataSlices);
                  }
                  else {
                      getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
              }
                  else {
                      gotAllSlices = false;
                      file.closeAsync();
                      app.showNotification("getSliceAsync Error:", sliceResult.error.message);
                  }
          });
      }

      function onGotAllSlices(docdataSlices) {
          var docdata = [];
          for (var i = 0; i < docdataSlices.length; i++) {
              docdata = docdata.concat(docdataSlices[i]);
          }

          var fileContent = new String();
          for (var j = 0; j < docdata.length; j++) {
              fileContent += String.fromCharCode(docdata[j]);
          }

          // Now all the file content is stored in 'fileContent' variable,
          // you can do something with it, such as print, fax...
      }

      // The following example gets the document in PDF format.
      Office.context.document.getFileAsync(Office.FileType.Pdf,
          function(result) {
              if (result.status == "succeeded") {
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);
                  // Now, you can call getSliceAsync to download the files,
                  // as described in the previous code segment (compressed format).
                  
                  myFile.closeAsync();
              }
              else {
                  app.showNotification("Error:", result.error.message);
              }
      }
      );
      ```
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="694d9-171">ファイルが返される形式を指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-171">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: options
          description: <span data-ttu-id="694d9-172">ドキュメントが分割されるスライスのサイズを設定するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-172">Provides options for setting the size of slices that the document will be divided into.</span></span>
          type:
            - office.Office.GetFileOptions
        - id: callback
          description: '<span data-ttu-id="694d9-173">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-173">Optional.</span></span> <span data-ttu-id="694d9-174">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-174">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-175">.</span><span class="sxs-lookup"><span data-stu-id="694d9-175"></span></span> <span data-ttu-id="694d9-176">result `value`のプロパティは、File オブジェクトです。</span><span class="sxs-lookup"><span data-stu-id="694d9-176">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFileAsync_1
    summary: '<span data-ttu-id="694d9-177">ドキュメント ファイル全体を、最大で 4194304 バイト (4 MB) のスライスに分割して返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-177">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="694d9-178">iOS 用アドインの場合は、最大 65536 バイト (64 KB) のファイル スライスがサポートされます。</span><span class="sxs-lookup"><span data-stu-id="694d9-178">For add-ins for iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="694d9-179">許容された制限を超えてファイル スライスのサイズを指定すると、"内部エラー" が発生する点に注意してください。</span><span class="sxs-lookup"><span data-stu-id="694d9-179">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="694d9-180">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-180">**Requirement sets**:</span></span>

      - <span data-ttu-id="694d9-181">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile)(を使用する場合`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-181">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-182">)</span><span class="sxs-lookup"><span data-stu-id="694d9-182"></span></span>

      - [<span data-ttu-id="694d9-183">ファイル</span><span class="sxs-lookup"><span data-stu-id="694d9-183">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="694d9-184">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile)(を使用する場合`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-184">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="694d9-185">)</span><span class="sxs-lookup"><span data-stu-id="694d9-185"></span></span>

      <span data-ttu-id="694d9-186">office for iOS 以外の office ホストアプリケーションで実行されているアドインの場合、getFileAsync メソッドは最大4194304バイト (4 MB) のスライスでのファイルの取得をサポートしています。</span><span class="sxs-lookup"><span data-stu-id="694d9-186">For add-ins running in Office host applications other than Office for iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="694d9-187">iOS アプリ向けの Office で実行するアドインの場合、getFileAsync メソッドは最大 65536 バイト (64 KB) にスライスしたファイルの取得をサポートします。</span><span class="sxs-lookup"><span data-stu-id="694d9-187">For add-ins running in Office for iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="694d9-188">filetype パラメーターは、 [Office の filetype](xref:office.Office.FileType)列挙型またはテキスト値を使用して指定できます。</span><span class="sxs-lookup"><span data-stu-id="694d9-188">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="694d9-189">ただし、ホストによって可能な値は次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="694d9-189">But the possible values vary with the host:</span></span>

      <span data-ttu-id="694d9-190">Windows デスクトップ、iPad、excel Online の場合:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-190">Excel for Windows desktop, iPad, and Excel Online: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="694d9-191">Excel for Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-191">Excel for Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-192">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-192"></span></span>

      <span data-ttu-id="694d9-193">powerpoint for Windows デスクトップ、Mac、iPad、powerpoint Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-193">PowerPoint for Windows desktop, Mac, iPad, and PowerPoint Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-194">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-194"></span></span>

      <span data-ttu-id="694d9-195">word for Windows デスクトップ、Mac、iPad、および word Online:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="694d9-195">Word for Windows desktop, Mac, iPad, and Word Online: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="694d9-196">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="694d9-196"></span></span><!-- --><span data-ttu-id="694d9-197">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-197"></span></span>
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="694d9-198">ファイルが返される形式を指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-198">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: callback
          description: '<span data-ttu-id="694d9-199">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-199">Optional.</span></span> <span data-ttu-id="694d9-200">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-200">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-201">.</span><span class="sxs-lookup"><span data-stu-id="694d9-201"></span></span> <span data-ttu-id="694d9-202">result `value`のプロパティは、File オブジェクトです。</span><span class="sxs-lookup"><span data-stu-id="694d9-202">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync
    summary: <span data-ttu-id="694d9-203">現在のドキュメントのファイル プロパティを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-203">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-204">**要件セット**:[セット内にない](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="694d9-204">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="694d9-205">url プロパティを使用して、ファイルの url を取得します。`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="694d9-205">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="694d9-206">.</span><span class="sxs-lookup"><span data-stu-id="694d9-206"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-207">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-207">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-208">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-208">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-209">.</span><span class="sxs-lookup"><span data-stu-id="694d9-209"></span></span> <span data-ttu-id="694d9-210">result `value`のプロパティは、ファイルのプロパティです (URL はにあります)。`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="694d9-210">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="694d9-211">).</span><span class="sxs-lookup"><span data-stu-id="694d9-211"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync_1
    summary: <span data-ttu-id="694d9-212">現在のドキュメントのファイル プロパティを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-212">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-213">**要件セット**:[セット内にない](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="694d9-213">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="694d9-214">url プロパティを使用して、ファイルの url を取得します。`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="694d9-214">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="694d9-215">.</span><span class="sxs-lookup"><span data-stu-id="694d9-215"></span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-216">例</span><span class="sxs-lookup"><span data-stu-id="694d9-216">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-217">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-217">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-218">.</span><span class="sxs-lookup"><span data-stu-id="694d9-218"></span></span> <span data-ttu-id="694d9-219">result `value`のプロパティは、ファイルのプロパティです (URL はにあります)。`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="694d9-219">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="694d9-220">).</span><span class="sxs-lookup"><span data-stu-id="694d9-220"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync
    summary: |-
      <span data-ttu-id="694d9-221">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-221">Project documents only.</span></span> <span data-ttu-id="694d9-222">現在のプロジェクト内のリソースのコレクションの最大インデックスを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-222">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="694d9-223">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-223">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-224">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-224">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-225">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-225">Optional.</span></span> <span data-ttu-id="694d9-226">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-226">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-227">.</span><span class="sxs-lookup"><span data-stu-id="694d9-227"></span></span> <span data-ttu-id="694d9-228">結果`value`のプロパティは、現在のプロジェクトのリソースコレクションの最大インデックス番号です。</span><span class="sxs-lookup"><span data-stu-id="694d9-228">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync_1
    summary: |-
      <span data-ttu-id="694d9-229">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-229">Project documents only.</span></span> <span data-ttu-id="694d9-230">現在のプロジェクト内のリソースのコレクションの最大インデックスを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-230">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="694d9-231">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-231">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-232">例</span><span class="sxs-lookup"><span data-stu-id="694d9-232">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-233">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-233">Optional.</span></span> <span data-ttu-id="694d9-234">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-234">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-235">.</span><span class="sxs-lookup"><span data-stu-id="694d9-235"></span></span> <span data-ttu-id="694d9-236">結果`value`のプロパティは、現在のプロジェクトのリソースコレクションの最大インデックス番号です。</span><span class="sxs-lookup"><span data-stu-id="694d9-236">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync
    summary: |-
      <span data-ttu-id="694d9-237">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-237">Project documents only.</span></span> <span data-ttu-id="694d9-238">現在のプロジェクト内のタスクのコレクションの最大インデックスを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-238">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="694d9-239">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-239">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-240">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-240">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-241">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-241">Optional.</span></span> <span data-ttu-id="694d9-242">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-242">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-243">.</span><span class="sxs-lookup"><span data-stu-id="694d9-243"></span></span> <span data-ttu-id="694d9-244">結果`value`のプロパティは、現在のプロジェクトのタスクコレクションの最大インデックス番号です。</span><span class="sxs-lookup"><span data-stu-id="694d9-244">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync_1
    summary: |-
      <span data-ttu-id="694d9-245">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-245">Project documents only.</span></span> <span data-ttu-id="694d9-246">現在のプロジェクト内のタスクのコレクションの最大インデックスを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-246">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="694d9-247">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-247">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-248">例</span><span class="sxs-lookup"><span data-stu-id="694d9-248">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-249">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-249">Optional.</span></span> <span data-ttu-id="694d9-250">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-250">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-251">.</span><span class="sxs-lookup"><span data-stu-id="694d9-251"></span></span> <span data-ttu-id="694d9-252">結果`value`のプロパティは、現在のプロジェクトのタスクコレクションの最大インデックス番号です。</span><span class="sxs-lookup"><span data-stu-id="694d9-252">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getProjectFieldAsync
    summary: <span data-ttu-id="694d9-253">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-253">Project documents only.</span></span> <span data-ttu-id="694d9-254">プロジェクトフィールドの取得 (例)</span><span class="sxs-lookup"><span data-stu-id="694d9-254">Get Project field (Ex.</span></span> <span data-ttu-id="694d9-255">projectwebaccessurl)。</span><span class="sxs-lookup"><span data-stu-id="694d9-255">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="694d9-256">プロジェクトレベルのフィールド。</span><span class="sxs-lookup"><span data-stu-id="694d9-256">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="694d9-257">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-257">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-258">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-258">Optional.</span></span> <span data-ttu-id="694d9-259">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-259">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-260">.</span><span class="sxs-lookup"><span data-stu-id="694d9-260"></span></span> <span data-ttu-id="694d9-261">result `value`のプロパティには、指定`fieldValue`されたフィールドの値を表すプロパティが含まれています。</span><span class="sxs-lookup"><span data-stu-id="694d9-261">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getProjectFieldAsync_1
    summary: <span data-ttu-id="694d9-262">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-262">Project documents only.</span></span> <span data-ttu-id="694d9-263">プロジェクトフィールドの取得 (例)</span><span class="sxs-lookup"><span data-stu-id="694d9-263">Get Project field (Ex.</span></span> <span data-ttu-id="694d9-264">projectwebaccessurl)。</span><span class="sxs-lookup"><span data-stu-id="694d9-264">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-265">例</span><span class="sxs-lookup"><span data-stu-id="694d9-265">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="694d9-266">プロジェクトレベルのフィールド。</span><span class="sxs-lookup"><span data-stu-id="694d9-266">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="694d9-267">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-267">Optional.</span></span> <span data-ttu-id="694d9-268">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-268">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-269">.</span><span class="sxs-lookup"><span data-stu-id="694d9-269"></span></span> <span data-ttu-id="694d9-270">result `value`のプロパティには、指定`fieldValue`されたフィールドの値を表すプロパティが含まれています。</span><span class="sxs-lookup"><span data-stu-id="694d9-270">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync
    summary: |-
      <span data-ttu-id="694d9-271">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-271">Project documents only.</span></span> <span data-ttu-id="694d9-272">リソースコレクション内の指定されたインデックスを持つリソースの GUID を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-272">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="694d9-273">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-273">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="694d9-274">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="694d9-274">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="694d9-275">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-275">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-276">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-276">Optional.</span></span> <span data-ttu-id="694d9-277">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-277">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-278">.</span><span class="sxs-lookup"><span data-stu-id="694d9-278"></span></span> <span data-ttu-id="694d9-279">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-279">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync_1
    summary: |-
      <span data-ttu-id="694d9-280">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-280">Project documents only.</span></span> <span data-ttu-id="694d9-281">リソースコレクション内の指定されたインデックスを持つリソースの GUID を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-281">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="694d9-282">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-282">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-283">例</span><span class="sxs-lookup"><span data-stu-id="694d9-283">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="694d9-284">The index of the resource in the collection of resources for the project.</span><span class="sxs-lookup"><span data-stu-id="694d9-284">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="694d9-285">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-285">Optional.</span></span> <span data-ttu-id="694d9-286">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-286">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-287">.</span><span class="sxs-lookup"><span data-stu-id="694d9-287"></span></span> <span data-ttu-id="694d9-288">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-288">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync
    summary: <span data-ttu-id="694d9-289">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-289">Project documents only.</span></span> <span data-ttu-id="694d9-290">提供されたリソース Id のリソースフィールドを取得します。 (例、)</span><span class="sxs-lookup"><span data-stu-id="694d9-290">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="694d9-291">リソース Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-291">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-292">リソースフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-292">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="694d9-293">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-293">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-294">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-294">Optional.</span></span> <span data-ttu-id="694d9-295">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-295">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-296">.</span><span class="sxs-lookup"><span data-stu-id="694d9-296"></span></span> <span data-ttu-id="694d9-297">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-297">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync_1
    summary: <span data-ttu-id="694d9-298">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-298">Project documents only.</span></span> <span data-ttu-id="694d9-299">提供されたリソース Id のリソースフィールドを取得します。 (例、)</span><span class="sxs-lookup"><span data-stu-id="694d9-299">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-300">例</span><span class="sxs-lookup"><span data-stu-id="694d9-300">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="694d9-301">リソース Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-301">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-302">リソースフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-302">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="694d9-303">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-303">Optional.</span></span> <span data-ttu-id="694d9-304">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-304">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-305">.</span><span class="sxs-lookup"><span data-stu-id="694d9-305"></span></span> <span data-ttu-id="694d9-306">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-306">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedDataAsync
    summary: <span data-ttu-id="694d9-307">ドキュメントの現在の選択範囲に含まれるデータを読み取ります。</span><span class="sxs-lookup"><span data-stu-id="694d9-307">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-308">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-308">**Requirement sets**:</span></span>

      - <span data-ttu-id="694d9-309">[htmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)(を使用する場合`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="694d9-309">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="694d9-310">)</span><span class="sxs-lookup"><span data-stu-id="694d9-310"></span></span>

      - <span data-ttu-id="694d9-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)(を使用する場合`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="694d9-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="694d9-312">)</span><span class="sxs-lookup"><span data-stu-id="694d9-312"></span></span>

      - <span data-ttu-id="694d9-313">[ooxmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)(を使用する場合`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="694d9-313">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="694d9-314">)</span><span class="sxs-lookup"><span data-stu-id="694d9-314"></span></span>

      - [<span data-ttu-id="694d9-315">Selection</span><span class="sxs-lookup"><span data-stu-id="694d9-315">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="694d9-316">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)(を使用する場合`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="694d9-316">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="694d9-317">)</span><span class="sxs-lookup"><span data-stu-id="694d9-317"></span></span>

      - <span data-ttu-id="694d9-318">[textcoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)(を使用する場合`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-318">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="694d9-319">)</span><span class="sxs-lookup"><span data-stu-id="694d9-319"></span></span>

      <span data-ttu-id="694d9-320">getselecteddataasync メソッドに渡されるコールバック関数では、AsyncResult オブジェクトのプロパティを使用して、次の情報を返すことができます。</span><span class="sxs-lookup"><span data-stu-id="694d9-320">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-321">プロパティ</span><span class="sxs-lookup"><span data-stu-id="694d9-321">Property</span></span></th> <th><span data-ttu-id="694d9-322">使用目的</span><span class="sxs-lookup"><span data-stu-id="694d9-322">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-323">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="694d9-323">AsyncResult.value</span></span></td> <td><span data-ttu-id="694d9-324">取得するオブジェクトまたはデータがないため、常に undefined を返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-324">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-325">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="694d9-325">AsyncResult.status</span></span></td> <td><span data-ttu-id="694d9-326">操作の成功または失敗を判断します。</span><span class="sxs-lookup"><span data-stu-id="694d9-326">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-327">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="694d9-327">AsyncResult.error</span></span></td> <td><span data-ttu-id="694d9-328">操作が失敗した場合、エラーに関する情報を提供する Error オブジェクトにアクセスします。</span><span class="sxs-lookup"><span data-stu-id="694d9-328">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-329">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="694d9-329">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="694d9-330">変更されずに AsyncResult オブジェクトで返される任意の型のユーザー定義項目。</span><span class="sxs-lookup"><span data-stu-id="694d9-330">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="694d9-331">[CoercionType](xref:office.Office.CoercionType)パラメーターに指定できる値は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-331">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-332">ホスト</span><span class="sxs-lookup"><span data-stu-id="694d9-332">Host</span></span></th> <th><span data-ttu-id="694d9-333">サポートされる coercionType</span><span class="sxs-lookup"><span data-stu-id="694d9-333">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-334">Excel、PowerPoint、Project、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-334">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="694d9-335">`Office.CoercionType.Text`表す</span><span class="sxs-lookup"><span data-stu-id="694d9-335">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-336">Excel および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-336">Excel and Word</span></span></td> <td><span data-ttu-id="694d9-337">`Office.CoercionType.Matrix`(配列の配列)</span><span class="sxs-lookup"><span data-stu-id="694d9-337">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-338">Access、Excel、Word</span><span class="sxs-lookup"><span data-stu-id="694d9-338">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="694d9-339">`Office.CoercionType.Table`(TableData オブジェクト)</span><span class="sxs-lookup"><span data-stu-id="694d9-339">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-340">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-340">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="694d9-341">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-341">Word</span></span></td> <td><span data-ttu-id="694d9-342">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="694d9-342">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-343">PowerPoint、PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="694d9-343">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="694d9-344">Excel、PowerPoint、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-344">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="694d9-345">例</span><span class="sxs-lookup"><span data-stu-id="694d9-345">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="694d9-346">返されるデータ構造の種類です。</span><span class="sxs-lookup"><span data-stu-id="694d9-346">The type of data structure to return.</span></span> <span data-ttu-id="694d9-347">各ホストでサポートされている強制型変換の種類については、「備考」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="694d9-347">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: options
          description: <span data-ttu-id="694d9-348">返されるデータとその書式設定方法をカスタマイズするためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-348">Provides options for customizing what data is returned and how it is formatted.</span></span>
          type:
            - office.Office.GetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="694d9-349">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-349">Optional.</span></span> <span data-ttu-id="694d9-350">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-350">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-351">.</span><span class="sxs-lookup"><span data-stu-id="694d9-351"></span></span> <span data-ttu-id="694d9-352">結果`value`のプロパティは、現在の選択範囲内のデータです。</span><span class="sxs-lookup"><span data-stu-id="694d9-352">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="694d9-353">これは、coercionType パラメーターで指定したデータ構造または形式で返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-353">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="694d9-354">(データの強制型変換の詳細については、「注釈」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="694d9-354">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedDataAsync_1
    summary: <span data-ttu-id="694d9-355">ドキュメントの現在の選択範囲に含まれるデータを読み取ります。</span><span class="sxs-lookup"><span data-stu-id="694d9-355">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-356">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-356">**Requirement sets**:</span></span>

      - <span data-ttu-id="694d9-357">[htmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)(を使用する場合`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="694d9-357">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="694d9-358">)</span><span class="sxs-lookup"><span data-stu-id="694d9-358"></span></span>

      - <span data-ttu-id="694d9-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)(を使用する場合`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="694d9-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="694d9-360">)</span><span class="sxs-lookup"><span data-stu-id="694d9-360"></span></span>

      - <span data-ttu-id="694d9-361">[ooxmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)(を使用する場合`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="694d9-361">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="694d9-362">)</span><span class="sxs-lookup"><span data-stu-id="694d9-362"></span></span>

      - [<span data-ttu-id="694d9-363">Selection</span><span class="sxs-lookup"><span data-stu-id="694d9-363">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="694d9-364">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)(を使用する場合`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="694d9-364">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="694d9-365">)</span><span class="sxs-lookup"><span data-stu-id="694d9-365"></span></span>

      - <span data-ttu-id="694d9-366">[textcoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)(を使用する場合`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-366">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="694d9-367">)</span><span class="sxs-lookup"><span data-stu-id="694d9-367"></span></span>

      <span data-ttu-id="694d9-368">getselecteddataasync メソッドに渡されるコールバック関数では、AsyncResult オブジェクトのプロパティを使用して、次の情報を返すことができます。</span><span class="sxs-lookup"><span data-stu-id="694d9-368">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-369">プロパティ</span><span class="sxs-lookup"><span data-stu-id="694d9-369">Property</span></span></th> <th><span data-ttu-id="694d9-370">使用目的</span><span class="sxs-lookup"><span data-stu-id="694d9-370">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-371">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="694d9-371">AsyncResult.value</span></span></td> <td><span data-ttu-id="694d9-372">取得するオブジェクトまたはデータがないため、常に undefined を返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-372">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-373">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="694d9-373">AsyncResult.status</span></span></td> <td><span data-ttu-id="694d9-374">操作の成功または失敗を判断します。</span><span class="sxs-lookup"><span data-stu-id="694d9-374">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-375">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="694d9-375">AsyncResult.error</span></span></td> <td><span data-ttu-id="694d9-376">操作が失敗した場合、エラーに関する情報を提供する Error オブジェクトにアクセスします。</span><span class="sxs-lookup"><span data-stu-id="694d9-376">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-377">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="694d9-377">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="694d9-378">変更されずに AsyncResult オブジェクトで返される任意の型のユーザー定義項目。</span><span class="sxs-lookup"><span data-stu-id="694d9-378">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="694d9-379">[CoercionType](xref:office.Office.CoercionType)パラメーターに指定できる値は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-379">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-380">ホスト</span><span class="sxs-lookup"><span data-stu-id="694d9-380">Host</span></span></th> <th><span data-ttu-id="694d9-381">サポートされる coercionType</span><span class="sxs-lookup"><span data-stu-id="694d9-381">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-382">Excel、PowerPoint、Project、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-382">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="694d9-383">`Office.CoercionType.Text`表す</span><span class="sxs-lookup"><span data-stu-id="694d9-383">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-384">Excel および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-384">Excel and Word</span></span></td> <td><span data-ttu-id="694d9-385">`Office.CoercionType.Matrix`(配列の配列)</span><span class="sxs-lookup"><span data-stu-id="694d9-385">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-386">Access、Excel、Word</span><span class="sxs-lookup"><span data-stu-id="694d9-386">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="694d9-387">`Office.CoercionType.Table`(TableData オブジェクト)</span><span class="sxs-lookup"><span data-stu-id="694d9-387">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-388">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-388">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="694d9-389">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-389">Word</span></span></td> <td><span data-ttu-id="694d9-390">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="694d9-390">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-391">PowerPoint、PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="694d9-391">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="694d9-392">Excel、PowerPoint、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-392">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="694d9-393">返されるデータ構造の種類です。</span><span class="sxs-lookup"><span data-stu-id="694d9-393">The type of data structure to return.</span></span> <span data-ttu-id="694d9-394">各ホストでサポートされている強制型変換の種類については、「備考」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="694d9-394">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: callback
          description: '<span data-ttu-id="694d9-395">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-395">Optional.</span></span> <span data-ttu-id="694d9-396">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-396">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-397">.</span><span class="sxs-lookup"><span data-stu-id="694d9-397"></span></span> <span data-ttu-id="694d9-398">結果`value`のプロパティは、現在の選択範囲内のデータです。</span><span class="sxs-lookup"><span data-stu-id="694d9-398">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="694d9-399">これは、coercionType パラメーターで指定したデータ構造または形式で返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-399">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="694d9-400">(データの強制型変換の詳細については、「注釈」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="694d9-400">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync
    summary: <span data-ttu-id="694d9-401">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-401">Project documents only.</span></span> <span data-ttu-id="694d9-402">現在選択されているリソースの Id を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-402">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-403">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-403">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-404">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-404">Optional.</span></span> <span data-ttu-id="694d9-405">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-405">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-406">.</span><span class="sxs-lookup"><span data-stu-id="694d9-406"></span></span> <span data-ttu-id="694d9-407">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-407">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync_1
    summary: <span data-ttu-id="694d9-408">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-408">Project documents only.</span></span> <span data-ttu-id="694d9-409">現在選択されているリソースの Id を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-409">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-410">例</span><span class="sxs-lookup"><span data-stu-id="694d9-410">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-411">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-411">Optional.</span></span> <span data-ttu-id="694d9-412">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-412">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-413">.</span><span class="sxs-lookup"><span data-stu-id="694d9-413"></span></span> <span data-ttu-id="694d9-414">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-414">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync
    summary: <span data-ttu-id="694d9-415">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-415">Project documents only.</span></span> <span data-ttu-id="694d9-416">現在選択されているタスクの Id を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-416">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-417">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-417">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-418">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-418">Optional.</span></span> <span data-ttu-id="694d9-419">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-419">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-420">.</span><span class="sxs-lookup"><span data-stu-id="694d9-420"></span></span> <span data-ttu-id="694d9-421">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-421">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync_1
    summary: <span data-ttu-id="694d9-422">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-422">Project documents only.</span></span> <span data-ttu-id="694d9-423">現在選択されているタスクの Id を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-423">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-424">例</span><span class="sxs-lookup"><span data-stu-id="694d9-424">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-425">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-425">Optional.</span></span> <span data-ttu-id="694d9-426">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-426">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-427">.</span><span class="sxs-lookup"><span data-stu-id="694d9-427"></span></span> <span data-ttu-id="694d9-428">result `value`のプロパティは、文字列としてのリソースの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-428">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedViewAsync
    summary: <span data-ttu-id="694d9-429">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-429">Project documents only.</span></span> <span data-ttu-id="694d9-430">現在選択されているビューの種類 (例) を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-430">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="694d9-431">ガント) とビュー名。</span><span class="sxs-lookup"><span data-stu-id="694d9-431">Gantt) and View Name.</span></span>
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-432">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-432">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-433">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-433">Optional.</span></span> <span data-ttu-id="694d9-434">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-434">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-435">.</span><span class="sxs-lookup"><span data-stu-id="694d9-435"></span></span> <span data-ttu-id="694d9-436">result `value`のプロパティには、次のプロパティが`viewName`含まれます:-ビューの名前 (projectviewtypes 定数)。</span><span class="sxs-lookup"><span data-stu-id="694d9-436">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="694d9-437">`viewType`-ビューの種類 (projectviewtypes 定数の整数値として)。</span><span class="sxs-lookup"><span data-stu-id="694d9-437">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getSelectedViewAsync_1
    summary: <span data-ttu-id="694d9-438">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-438">Project documents only.</span></span> <span data-ttu-id="694d9-439">現在選択されているビューの種類 (例) を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-439">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="694d9-440">ガント) とビュー名。</span><span class="sxs-lookup"><span data-stu-id="694d9-440">Gantt) and View Name.</span></span>
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-441">例</span><span class="sxs-lookup"><span data-stu-id="694d9-441">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-442">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-442">Optional.</span></span> <span data-ttu-id="694d9-443">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-443">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-444">.</span><span class="sxs-lookup"><span data-stu-id="694d9-444"></span></span> <span data-ttu-id="694d9-445">result `value`のプロパティには、次のプロパティが`viewName`含まれます:-ビューの名前 (projectviewtypes 定数)。</span><span class="sxs-lookup"><span data-stu-id="694d9-445">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="694d9-446">`viewType`-ビューの種類 (projectviewtypes 定数の整数値として)。</span><span class="sxs-lookup"><span data-stu-id="694d9-446">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync
    summary: '<span data-ttu-id="694d9-447">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-447">Project documents only.</span></span> <span data-ttu-id="694d9-448">指定した taskId のタスク名、WSS タスク Id、および ResourceNames を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-448">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-449">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-449">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="694d9-450">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-450">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-451">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-451">Optional.</span></span> <span data-ttu-id="694d9-452">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-452">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-453">.</span><span class="sxs-lookup"><span data-stu-id="694d9-453"></span></span> <span data-ttu-id="694d9-454">result `value`のプロパティには、次のプロパティが`taskName`含まれます:-タスクの名前。</span><span class="sxs-lookup"><span data-stu-id="694d9-454">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="694d9-455">`wssTaskId`-同期された SharePoint タスクリスト内のタスクの ID。</span><span class="sxs-lookup"><span data-stu-id="694d9-455">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="694d9-456">プロジェクトが SharePoint タスク リストと同期されていない場合、値は 0 です。</span><span class="sxs-lookup"><span data-stu-id="694d9-456">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="694d9-457">`resourceNames`-タスクに割り当てられているリソースの名前のコンマ区切りのリスト。</span><span class="sxs-lookup"><span data-stu-id="694d9-457">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync_1
    summary: '<span data-ttu-id="694d9-458">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-458">Project documents only.</span></span> <span data-ttu-id="694d9-459">指定した taskId のタスク名、WSS タスク Id、および ResourceNames を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-459">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-460">例</span><span class="sxs-lookup"><span data-stu-id="694d9-460">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-461">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-461">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="694d9-462">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-462">Optional.</span></span> <span data-ttu-id="694d9-463">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-463">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-464">.</span><span class="sxs-lookup"><span data-stu-id="694d9-464"></span></span> <span data-ttu-id="694d9-465">result `value`のプロパティには、次のプロパティが`taskName`含まれます:-タスクの名前。</span><span class="sxs-lookup"><span data-stu-id="694d9-465">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="694d9-466">`wssTaskId`-同期された SharePoint タスクリスト内のタスクの ID。</span><span class="sxs-lookup"><span data-stu-id="694d9-466">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="694d9-467">プロジェクトが SharePoint タスク リストと同期されていない場合、値は 0 です。</span><span class="sxs-lookup"><span data-stu-id="694d9-467">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="694d9-468">`resourceNames`-タスクに割り当てられているリソースの名前のコンマ区切りのリスト。</span><span class="sxs-lookup"><span data-stu-id="694d9-468">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync
    summary: |-
      <span data-ttu-id="694d9-469">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-469">Project documents only.</span></span> <span data-ttu-id="694d9-470">タスクコレクション内の指定されたインデックスを持つタスクの GUID を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-470">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="694d9-471">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-471">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="694d9-472">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="694d9-472">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="694d9-473">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-473">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-474">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-474">Optional.</span></span> <span data-ttu-id="694d9-475">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-475">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-476">.</span><span class="sxs-lookup"><span data-stu-id="694d9-476"></span></span> <span data-ttu-id="694d9-477">結果`value`のプロパティは、文字列としてのタスクの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-477">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync_1
    summary: |-
      <span data-ttu-id="694d9-478">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-478">Project documents only.</span></span> <span data-ttu-id="694d9-479">タスクコレクション内の指定されたインデックスを持つタスクの GUID を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-479">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="694d9-480">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-480">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-481">例</span><span class="sxs-lookup"><span data-stu-id="694d9-481">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="694d9-482">The index of the task in the collection of tasks for the project.</span><span class="sxs-lookup"><span data-stu-id="694d9-482">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="694d9-483">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-483">Optional.</span></span> <span data-ttu-id="694d9-484">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-484">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-485">.</span><span class="sxs-lookup"><span data-stu-id="694d9-485"></span></span> <span data-ttu-id="694d9-486">結果`value`のプロパティは、文字列としてのタスクの GUID です。</span><span class="sxs-lookup"><span data-stu-id="694d9-486">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskFieldAsync
    summary: <span data-ttu-id="694d9-487">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-487">Project documents only.</span></span> <span data-ttu-id="694d9-488">提供されたタスク Id のタスクフィールドを取得します。 (例</span><span class="sxs-lookup"><span data-stu-id="694d9-488">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="694d9-489">StartDate)。</span><span class="sxs-lookup"><span data-stu-id="694d9-489">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-490">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-490">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-491">タスクフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-491">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="694d9-492">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-492">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-493">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-493">Optional.</span></span> <span data-ttu-id="694d9-494">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-494">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-495">.</span><span class="sxs-lookup"><span data-stu-id="694d9-495"></span></span> <span data-ttu-id="694d9-496">result `value`のプロパティには、指定`fieldValue`されたフィールドの値を表すプロパティが含まれています。</span><span class="sxs-lookup"><span data-stu-id="694d9-496">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskFieldAsync_1
    summary: <span data-ttu-id="694d9-497">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-497">Project documents only.</span></span> <span data-ttu-id="694d9-498">提供されたタスク Id のタスクフィールドを取得します。 (例</span><span class="sxs-lookup"><span data-stu-id="694d9-498">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="694d9-499">StartDate)。</span><span class="sxs-lookup"><span data-stu-id="694d9-499">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-500">例</span><span class="sxs-lookup"><span data-stu-id="694d9-500">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-501">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-501">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-502">タスクフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-502">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="694d9-503">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-503">Optional.</span></span> <span data-ttu-id="694d9-504">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-504">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-505">.</span><span class="sxs-lookup"><span data-stu-id="694d9-505"></span></span> <span data-ttu-id="694d9-506">result `value`のプロパティには、指定`fieldValue`されたフィールドの値を表すプロパティが含まれています。</span><span class="sxs-lookup"><span data-stu-id="694d9-506">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync
    summary: '<span data-ttu-id="694d9-507">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-507">Project documents only.</span></span> <span data-ttu-id="694d9-508">タスクリストの WSS Url とリスト名を取得します。 MPP も同期されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-508">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="694d9-509">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-509">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-510">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-510">Optional.</span></span> <span data-ttu-id="694d9-511">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-511">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-512">.</span><span class="sxs-lookup"><span data-stu-id="694d9-512"></span></span> <span data-ttu-id="694d9-513">結果`value`のプロパティには、次のプロパティが`listName`含まれます:-同期された SharePoint タスクリストの名前。</span><span class="sxs-lookup"><span data-stu-id="694d9-513">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="694d9-514">`serverUrl`-同期された SharePoint タスクリストの URL。</span><span class="sxs-lookup"><span data-stu-id="694d9-514">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync_1
    summary: '<span data-ttu-id="694d9-515">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-515">Project documents only.</span></span> <span data-ttu-id="694d9-516">タスクリストの WSS Url とリスト名を取得します。 MPP も同期されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-516">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="694d9-517">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-517">Optional.</span></span> <span data-ttu-id="694d9-518">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-518">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-519">.</span><span class="sxs-lookup"><span data-stu-id="694d9-519"></span></span> <span data-ttu-id="694d9-520">結果`value`のプロパティには、次のプロパティが`listName`含まれます:-同期された SharePoint タスクリストの名前。</span><span class="sxs-lookup"><span data-stu-id="694d9-520">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="694d9-521">`serverUrl`-同期された SharePoint タスクリストの URL。</span><span class="sxs-lookup"><span data-stu-id="694d9-521">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync
    summary: <span data-ttu-id="694d9-522">ドキュメント内の指定されたオブジェクトまたは場所に移動します。</span><span class="sxs-lookup"><span data-stu-id="694d9-522">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-523">**要件セット**:[セットに含まれない](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="694d9-523">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="694d9-524">PowerPoint では、マスター表示で document.gotobyidasync メソッドはサポートされていません。</span><span class="sxs-lookup"><span data-stu-id="694d9-524">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="694d9-525">selectionMode オプションが原因で発生する動作は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-525">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="694d9-526">Excel の場合`Office.SelectionMode.Selected` : バインド内のすべてのコンテンツ、または名前付きアイテムを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-526">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="694d9-527">Office.SelectionMode.None では、テキスト バインドの場合は、セルを選択します。マトリックス バインド、テーブル バインド、および名前付きアイテムの場合は、最初のデータ セルを選択します (テーブルの見出し行の最初のセルではありません)。</span><span class="sxs-lookup"><span data-stu-id="694d9-527">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="694d9-528">PowerPoint では`Office.SelectionMode.Selected` 、スライドのタイトルまたは最初のテキストボックスを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-528">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="694d9-529">`Office.SelectionMode.None`何も選択しません。</span><span class="sxs-lookup"><span data-stu-id="694d9-529">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="694d9-530">Word では`Office.SelectionMode.Selected` 、バインド内のすべてのコンテンツを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-530">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="694d9-531">Office.SelectionMode.None では、テキスト バインドの場合はテキストの最初までカーソルを移動します。マトリックス バインドとテーブル バインドの場合は、最初のデータ セルを選択します (テーブルの見出し行の最初のセルではありません)。</span><span class="sxs-lookup"><span data-stu-id="694d9-531">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="694d9-532">移動先のオブジェクトまたは場所の識別子です。</span><span class="sxs-lookup"><span data-stu-id="694d9-532">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="694d9-533">移動先の場所の型です。</span><span class="sxs-lookup"><span data-stu-id="694d9-533">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: options
          description: <span data-ttu-id="694d9-534">移動先の場所を選択するかどうかのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-534">Provides options for whether to select the location that is navigated to.</span></span>
          type:
            - office.Office.GoToByIdOptions
        - id: callback
          description: '<span data-ttu-id="694d9-535">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-535">Optional.</span></span> <span data-ttu-id="694d9-536">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-536">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-537">.</span><span class="sxs-lookup"><span data-stu-id="694d9-537"></span></span> <span data-ttu-id="694d9-538">結果`value`のプロパティは現在のビューです。</span><span class="sxs-lookup"><span data-stu-id="694d9-538">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync_1
    summary: <span data-ttu-id="694d9-539">ドキュメント内の指定されたオブジェクトまたは場所に移動します。</span><span class="sxs-lookup"><span data-stu-id="694d9-539">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-540">**要件セット**:[セットに含まれない](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="694d9-540">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="694d9-541">PowerPoint では、マスター表示で document.gotobyidasync メソッドはサポートされていません。</span><span class="sxs-lookup"><span data-stu-id="694d9-541">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="694d9-542">selectionMode オプションが原因で発生する動作は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-542">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="694d9-543">Excel の場合`Office.SelectionMode.Selected` : バインド内のすべてのコンテンツ、または名前付きアイテムを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-543">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="694d9-544">Office.SelectionMode.None では、テキスト バインドの場合は、セルを選択します。マトリックス バインド、テーブル バインド、および名前付きアイテムの場合は、最初のデータ セルを選択します (テーブルの見出し行の最初のセルではありません)。</span><span class="sxs-lookup"><span data-stu-id="694d9-544">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="694d9-545">PowerPoint では`Office.SelectionMode.Selected` 、スライドのタイトルまたは最初のテキストボックスを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-545">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="694d9-546">`Office.SelectionMode.None`何も選択しません。</span><span class="sxs-lookup"><span data-stu-id="694d9-546">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="694d9-547">Word では`Office.SelectionMode.Selected` 、バインド内のすべてのコンテンツを選択します。</span><span class="sxs-lookup"><span data-stu-id="694d9-547">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="694d9-548">Office.SelectionMode.None では、テキスト バインドの場合はテキストの最初までカーソルを移動します。マトリックス バインドとテーブル バインドの場合は、最初のデータ セルを選択します (テーブルの見出し行の最初のセルではありません)。</span><span class="sxs-lookup"><span data-stu-id="694d9-548">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-549">例</span><span class="sxs-lookup"><span data-stu-id="694d9-549">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="694d9-550">移動先のオブジェクトまたは場所の識別子です。</span><span class="sxs-lookup"><span data-stu-id="694d9-550">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="694d9-551">移動先の場所の型です。</span><span class="sxs-lookup"><span data-stu-id="694d9-551">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: callback
          description: '<span data-ttu-id="694d9-552">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-552">Optional.</span></span> <span data-ttu-id="694d9-553">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-553">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-554">.</span><span class="sxs-lookup"><span data-stu-id="694d9-554"></span></span> <span data-ttu-id="694d9-555">結果`value`のプロパティは現在のビューです。</span><span class="sxs-lookup"><span data-stu-id="694d9-555">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.removeHandlerAsync
    summary: <span data-ttu-id="694d9-556">指定したイベントの種類のイベントハンドラーを削除します。</span><span class="sxs-lookup"><span data-stu-id="694d9-556">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="694d9-557">**要件セット**: [documentevents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="694d9-557">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="694d9-558">イベントの種類。</span><span class="sxs-lookup"><span data-stu-id="694d9-558">The event type.</span></span> <span data-ttu-id="694d9-559">ドキュメントの場合は、' ドキュメント. selectionchanged ' または ' ドキュメント. activeviewchanged ' のいずれかです。</span><span class="sxs-lookup"><span data-stu-id="694d9-559">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: options
          description: <span data-ttu-id="694d9-560">削除するイベントハンドラーを決めるためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-560">Provides options to determine which event handler or handlers are removed.</span></span>
          type:
            - office.Office.RemoveHandlerOptions
        - id: callback
          description: '<span data-ttu-id="694d9-561">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-561">Optional.</span></span> <span data-ttu-id="694d9-562">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-562">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-563">.</span><span class="sxs-lookup"><span data-stu-id="694d9-563"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.removeHandlerAsync_1
    summary: <span data-ttu-id="694d9-564">指定したイベントの種類のイベントハンドラーを削除します。</span><span class="sxs-lookup"><span data-stu-id="694d9-564">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-565">**要件セット**: [documentevents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="694d9-565">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-566">例</span><span class="sxs-lookup"><span data-stu-id="694d9-566">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="694d9-567">イベントの種類。</span><span class="sxs-lookup"><span data-stu-id="694d9-567">The event type.</span></span> <span data-ttu-id="694d9-568">ドキュメントの場合は、' ドキュメント. selectionchanged ' または ' ドキュメント. activeviewchanged ' のいずれかです。</span><span class="sxs-lookup"><span data-stu-id="694d9-568">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: callback
          description: '<span data-ttu-id="694d9-569">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-569">Optional.</span></span> <span data-ttu-id="694d9-570">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-570">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-571">.</span><span class="sxs-lookup"><span data-stu-id="694d9-571"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync
    summary: |-
      <span data-ttu-id="694d9-572">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-572">Project documents only.</span></span> <span data-ttu-id="694d9-573">指定されたリソース Id のリソースフィールドを設定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-573">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="694d9-574">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-574">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="694d9-575">リソース Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-575">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-576">リソースフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-576">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="694d9-577">ターゲットフィールドの値。</span><span class="sxs-lookup"><span data-stu-id="694d9-577">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="694d9-578">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-578">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-579">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-579">Optional.</span></span> <span data-ttu-id="694d9-580">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-580">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-581">.</span><span class="sxs-lookup"><span data-stu-id="694d9-581"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync_1
    summary: |-
      <span data-ttu-id="694d9-582">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-582">Project documents only.</span></span> <span data-ttu-id="694d9-583">指定されたリソース Id のリソースフィールドを設定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-583">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="694d9-584">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-584">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-585">例</span><span class="sxs-lookup"><span data-stu-id="694d9-585">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="694d9-586">リソース Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-586">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-587">リソースフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-587">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="694d9-588">ターゲットフィールドの値。</span><span class="sxs-lookup"><span data-stu-id="694d9-588">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="694d9-589">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-589">Optional.</span></span> <span data-ttu-id="694d9-590">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-590">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-591">.</span><span class="sxs-lookup"><span data-stu-id="694d9-591"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync
    summary: <span data-ttu-id="694d9-592">指定されたデータを現在の選択範囲に書き込みます。</span><span class="sxs-lookup"><span data-stu-id="694d9-592">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-593">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-593">**Requirement sets**:</span></span>

      - [<span data-ttu-id="694d9-594">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="694d9-594">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="694d9-595">、(を使用する場合)`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="694d9-595">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="694d9-596">)</span><span class="sxs-lookup"><span data-stu-id="694d9-596"></span></span>

      - <span data-ttu-id="694d9-597">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion)(を使用する場合`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="694d9-597">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="694d9-598">)</span><span class="sxs-lookup"><span data-stu-id="694d9-598"></span></span>

      - <span data-ttu-id="694d9-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)(を使用する場合`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="694d9-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="694d9-600">)</span><span class="sxs-lookup"><span data-stu-id="694d9-600"></span></span>

      - <span data-ttu-id="694d9-601">[ooxmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)(を使用する場合`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="694d9-601">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="694d9-602">)</span><span class="sxs-lookup"><span data-stu-id="694d9-602"></span></span>

      - [<span data-ttu-id="694d9-603">Selection</span><span class="sxs-lookup"><span data-stu-id="694d9-603">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="694d9-604">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)(を使用する場合`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="694d9-604">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="694d9-605">)</span><span class="sxs-lookup"><span data-stu-id="694d9-605"></span></span>

      - <span data-ttu-id="694d9-606">[textcoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)(を使用する場合`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-606">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="694d9-607">)</span><span class="sxs-lookup"><span data-stu-id="694d9-607"></span></span>

      <span data-ttu-id="694d9-608">**アプリケーション固有の動作**</span><span class="sxs-lookup"><span data-stu-id="694d9-608">**Application-specific behaviors**</span></span>

      <span data-ttu-id="694d9-609">選択範囲にデータを書き込むときには、次のアプリケーション固有のアクションが適用されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-609">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="694d9-610">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-610">Word</span></span></td> <td><span data-ttu-id="694d9-611">選択範囲がなく、挿入ポイントが有効な位置にある場合は、指定`data`されたが挿入ポイントに挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-611">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="694d9-612">が`data`文字列の場合、指定したテキストが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-612">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-613">が`data`配列の配列 ("matrix") または TableData オブジェクトの場合は、新しい Word の表が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-613">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-614">が`data` html の場合は、指定された html が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-614">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="694d9-615">(**重要**: 挿入する HTML のいずれかが無効な場合は、Word でエラーは発生しません。</span><span class="sxs-lookup"><span data-stu-id="694d9-615">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="694d9-616">Word は、できるだけ多くの HTML を挿入し、無効なデータを省略します。</span><span class="sxs-lookup"><span data-stu-id="694d9-616">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-617">Office `data` Open xml の場合、指定した xml が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-617">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-618">が`data` base64 でエンコードされたイメージストリームの場合、指定したイメージが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-618">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-619">選択範囲がある場合</span><span class="sxs-lookup"><span data-stu-id="694d9-619">If there is a selection</span></span></td> <td><span data-ttu-id="694d9-620">これは、上記と同じ規則`data`に従って、次のように置き換えられます。</span><span class="sxs-lookup"><span data-stu-id="694d9-620">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-621">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-621">Insert images</span></span></td> <td><span data-ttu-id="694d9-622">挿入された画像はインラインで配置されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-622">Inserted images are placed inline.</span></span> <span data-ttu-id="694d9-623">imageLeft パラメーターと imageTop パラメーターは無視されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-623">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="694d9-624">画像の縦横比は常に固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-624">The image aspect ratio is always locked.</span></span> <span data-ttu-id="694d9-625">imageWidth パラメーターと imageHeight パラメーターのいずれか 1 つのみが指定された場合、もう一方の値がスケーリングされて自動的に元の縦横比が維持されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-625">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-626">Excel</span><span class="sxs-lookup"><span data-stu-id="694d9-626">Excel</span></span></td> <td><span data-ttu-id="694d9-627">単一のセルが選択されている場合</span><span class="sxs-lookup"><span data-stu-id="694d9-627">If a single cell is selected</span></span></td> <td><span data-ttu-id="694d9-628">が`data`文字列の場合、指定したテキストが現在のセルの値として挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-628">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-629">が`data`配列の配列 ("matrix") の場合は、指定された行と列のセットが挿入されます (周囲のセルに含まれるデータが上書きされる場合は除く)。</span><span class="sxs-lookup"><span data-stu-id="694d9-629">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-630">が`data` TableData オブジェクトの場合、指定された行とヘッダーを持つ新しい Excel テーブルが挿入されます (周囲のセルに含まれるデータが上書きされる場合は除く)。</span><span class="sxs-lookup"><span data-stu-id="694d9-630">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-631">複数のセルが選択されている場合</span><span class="sxs-lookup"><span data-stu-id="694d9-631">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="694d9-632">、エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-632">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="694d9-633">、選択されているセルの値が更新されます。`data`</span><span class="sxs-lookup"><span data-stu-id="694d9-633">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="694d9-634">.</span><span class="sxs-lookup"><span data-stu-id="694d9-634"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-635">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-635">Insert images</span></span></td> <td><span data-ttu-id="694d9-636">挿入された画像は浮動になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-636">Inserted images are floating.</span></span> <span data-ttu-id="694d9-637">位置パラメーターの imageLeft と imageTop は、現在選択されているセルからの相対位置になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-637">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="694d9-638">imageLeft と imageTop は負の値にすることもでき、その場合は、画像がワークシート内に収まるようにするために Excel によって再調整される可能性があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-638">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="694d9-639">画像の縦横比は、 imageWidth と imageHeight パラメーターの両方が指定されない限り固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-639">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="694d9-640">imageWidth パラメーターと imageHeight パラメーターのいずれか一方が指定されている場合、もう一方の値は、元の縦横比を維持するように自動調整されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-640">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-641">その他すべてのケース</span><span class="sxs-lookup"><span data-stu-id="694d9-641">All other cases</span></span></td> <td><span data-ttu-id="694d9-642">エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-642">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-643">Excel Online</span><span class="sxs-lookup"><span data-stu-id="694d9-643">Excel Online</span></span></td> <td><span data-ttu-id="694d9-644">上記の excel で説明されている動作に加えて、これらの制限は excel Online でのデータの書き込み時に適用されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-644">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="694d9-645">この`data`パラメーターを使用してワークシートに書き込むセルの合計数は、このメソッドに対する単一の呼び出しでは2万を超えることはできません。</span><span class="sxs-lookup"><span data-stu-id="694d9-645">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-646">`cellFormat`パラメーターに渡される書式設定グループの数が100を超えることはできません。</span><span class="sxs-lookup"><span data-stu-id="694d9-646">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="694d9-647">1 つの書式設定グループは、指定のセル範囲に適用される書式設定のセットから構成されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-647">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-648">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="694d9-648">PowerPoint</span></span></td> <td><span data-ttu-id="694d9-649">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-649">Insert image</span></span></td> <td><span data-ttu-id="694d9-650">挿入された画像は浮動になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-650">Inserted images are floating.</span></span> <span data-ttu-id="694d9-651">position imageLeft および imagetop パラメーターは省略可能ですが、指定した場合は両方とも存在する必要があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-651">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="694d9-652">1 つの値しか指定されない場合、それは無視されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-652">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="694d9-653">imageLeft と imageTop は負の値にすることもでき、その場合は、画像がスライドの外に配置される可能性があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-653">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="694d9-654">オプションのパラメーターが指定されず、スライドにプレースホルダがある場合は、画像によってスライドのプレースホルダが置き換えられます。</span><span class="sxs-lookup"><span data-stu-id="694d9-654">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="694d9-655">画像の縦横比は、 imageWidth パラメーターと imageHeight パラメーターの両方が指定されない限り固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-655">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="694d9-656">imageWidth パラメーターと imageHeight パラメーターのいずれか一方が指定されている場合、もう一方の値は、元の縦横比を維持するように自動調整されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-656">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="694d9-657">[CoercionType](xref:office.Office.CoercionType)パラメーターに指定できる値は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-657">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-658">ホスト</span><span class="sxs-lookup"><span data-stu-id="694d9-658">Host</span></span></th> <th><span data-ttu-id="694d9-659">サポートされる coercionType</span><span class="sxs-lookup"><span data-stu-id="694d9-659">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-660">Excel、PowerPoint、Project、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-660">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="694d9-661">`Office.CoercionType.Text`表す</span><span class="sxs-lookup"><span data-stu-id="694d9-661">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-662">Excel および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-662">Excel and Word</span></span></td> <td><span data-ttu-id="694d9-663">`Office.CoercionType.Matrix`(配列の配列)</span><span class="sxs-lookup"><span data-stu-id="694d9-663">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-664">Access、Excel、Word</span><span class="sxs-lookup"><span data-stu-id="694d9-664">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="694d9-665">`Office.CoercionType.Table`(TableData オブジェクト)</span><span class="sxs-lookup"><span data-stu-id="694d9-665">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-666">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-666">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="694d9-667">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-667">Word</span></span></td> <td><span data-ttu-id="694d9-668">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="694d9-668">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-669">PowerPoint、PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="694d9-669">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="694d9-670">Excel、PowerPoint、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-670">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="694d9-671">例</span><span class="sxs-lookup"><span data-stu-id="694d9-671">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="694d9-672">設定するデータを指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-672">The data to be set.</span></span> <span data-ttu-id="694d9-673">文字列または[CoercionType](xref:office.Office.CoercionType)の値、2d 配列、TableData オブジェクトのいずれかを指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-673">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="694d9-674">渡された値が`data`である場合は、次のようになります。</span><span class="sxs-lookup"><span data-stu-id="694d9-674">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="694d9-675">文字列: プレーン テキスト、または string に強制的に変換できるその他の値が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-675">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="694d9-676">Excel では、選択したセルに数式を追加する有効な数式としてデータを指定することもできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-676">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="694d9-677">例えば、data を "=SUM(A1:A5)" と設定すると、指定の範囲内の値が集計されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-677">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="694d9-678">ただし、バインドされたセルで数式を設定する場合、その後、バインドされたセルからは追加された数式 (または既存の数式) を読み取ることができません。</span><span class="sxs-lookup"><span data-stu-id="694d9-678">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="694d9-679">選択したセルで Document.getSelectedDataAsync メソッドを呼び出してそのデータを読み取ると、このメソッドは (数式の結果である) セルに表示されたデータのみを返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-679">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="694d9-680">配列の配列 ("matrix"): ヘッダーなしの表形式データが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-680">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="694d9-681">たとえば、2つの列に3つの行のデータを書き込むには、次のよう\[ \[な配列を渡します。 "\]R1C1 \["、"R1C2"、"\]R2C1 \["、"R2C2"、"\]\]R3C1"、"R3C2"。</span><span class="sxs-lookup"><span data-stu-id="694d9-681">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="694d9-682">3つの行からなる1つの列を記述するには\[ \[、次の\]よう\[な配列を\]渡し\[ます。 "R1C1"、"R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="694d9-682">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="694d9-683">Excel では、データを、選択したセルに追加する有効な数式を含む配列の配列として指定することもできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-683">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="694d9-684">たとえば、他のデータが上書きされない場合は、 \[ \[データを "= SUM (a1: a5)", "= AVERAGE (a1: a5\] \] )" に設定すると、選択範囲に2つの数式が追加されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-684">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="694d9-685">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span><span class="sxs-lookup"><span data-stu-id="694d9-685">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="694d9-686">TableData オブジェクト: ヘッダー付きのテーブルが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-686">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="694d9-687">excel では、data パラメーターに渡す TableData オブジェクトに数式を指定すると、excel の "集計列" 機能によって期待した結果が得られない場合があります。これにより、列内の数式は自動的に複製されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-687">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="694d9-688">この問題を回避するには、選択`data`したテーブルに数式が含まれている場合は、TableData オブジェクトではなく配列の配列としてデータを指定し、coercionType または "matrix" として指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-688">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="694d9-689">ただし、この手法では、次のいずれかの条件が満たされている場合にのみ、"計算列" 機能がブロックされます。 (1) 列のすべてのセルに対して、または (2) 列に2つ以上の数式が既に存在します。</span><span class="sxs-lookup"><span data-stu-id="694d9-689">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: options
          description: <span data-ttu-id="694d9-690">選択範囲にデータを挿入する方法のオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-690">Provides options for how to insert data to the selection.</span></span>
          type:
            - office.Office.SetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="694d9-691">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-691">Optional.</span></span> <span data-ttu-id="694d9-692">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-692">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-693">.</span><span class="sxs-lookup"><span data-stu-id="694d9-693"></span></span> <span data-ttu-id="694d9-694">取得するオブジェクトまたはデータがないため、AsyncResult プロパティは常に undefined を返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-694">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync_1
    summary: <span data-ttu-id="694d9-695">指定されたデータを現在の選択範囲に書き込みます。</span><span class="sxs-lookup"><span data-stu-id="694d9-695">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-696">**要件セット**:</span><span class="sxs-lookup"><span data-stu-id="694d9-696">**Requirement sets**:</span></span>

      - [<span data-ttu-id="694d9-697">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="694d9-697">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="694d9-698">、(を使用する場合)`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="694d9-698">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="694d9-699">)</span><span class="sxs-lookup"><span data-stu-id="694d9-699"></span></span>

      - <span data-ttu-id="694d9-700">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion)(を使用する場合`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="694d9-700">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="694d9-701">)</span><span class="sxs-lookup"><span data-stu-id="694d9-701"></span></span>

      - <span data-ttu-id="694d9-702">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion)(を使用する場合`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="694d9-702">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="694d9-703">)</span><span class="sxs-lookup"><span data-stu-id="694d9-703"></span></span>

      - <span data-ttu-id="694d9-704">[ooxmlcoercion 型変換](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion)(を使用する場合`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="694d9-704">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="694d9-705">)</span><span class="sxs-lookup"><span data-stu-id="694d9-705"></span></span>

      - [<span data-ttu-id="694d9-706">Selection</span><span class="sxs-lookup"><span data-stu-id="694d9-706">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="694d9-707">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion)(を使用する場合`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="694d9-707">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="694d9-708">)</span><span class="sxs-lookup"><span data-stu-id="694d9-708"></span></span>

      - <span data-ttu-id="694d9-709">[textcoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion)(を使用する場合`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="694d9-709">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="694d9-710">)</span><span class="sxs-lookup"><span data-stu-id="694d9-710"></span></span>

      <span data-ttu-id="694d9-711">**アプリケーション固有の動作**</span><span class="sxs-lookup"><span data-stu-id="694d9-711">**Application-specific behaviors**</span></span>

      <span data-ttu-id="694d9-712">選択範囲にデータを書き込むときには、次のアプリケーション固有のアクションが適用されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-712">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="694d9-713">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-713">Word</span></span></td> <td><span data-ttu-id="694d9-714">選択範囲がなく、挿入ポイントが有効な位置にある場合は、指定`data`されたが挿入ポイントに挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-714">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="694d9-715">が`data`文字列の場合、指定したテキストが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-715">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-716">が`data`配列の配列 ("matrix") または TableData オブジェクトの場合は、新しい Word の表が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-716">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-717">が`data` html の場合は、指定された html が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-717">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="694d9-718">(**重要**: 挿入する HTML のいずれかが無効な場合は、Word でエラーは発生しません。</span><span class="sxs-lookup"><span data-stu-id="694d9-718">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="694d9-719">Word は、できるだけ多くの HTML を挿入し、無効なデータを省略します。</span><span class="sxs-lookup"><span data-stu-id="694d9-719">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-720">Office `data` Open xml の場合、指定した xml が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-720">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-721">が`data` base64 でエンコードされたイメージストリームの場合、指定したイメージが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-721">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-722">選択範囲がある場合</span><span class="sxs-lookup"><span data-stu-id="694d9-722">If there is a selection</span></span></td> <td><span data-ttu-id="694d9-723">これは、上記と同じ規則`data`に従って、次のように置き換えられます。</span><span class="sxs-lookup"><span data-stu-id="694d9-723">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-724">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-724">Insert images</span></span></td> <td><span data-ttu-id="694d9-725">挿入された画像はインラインで配置されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-725">Inserted images are placed inline.</span></span> <span data-ttu-id="694d9-726">imageLeft パラメーターと imageTop パラメーターは無視されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-726">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="694d9-727">画像の縦横比は常に固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-727">The image aspect ratio is always locked.</span></span> <span data-ttu-id="694d9-728">imageWidth パラメーターと imageHeight パラメーターのいずれか 1 つのみが指定された場合、もう一方の値がスケーリングされて自動的に元の縦横比が維持されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-728">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-729">Excel</span><span class="sxs-lookup"><span data-stu-id="694d9-729">Excel</span></span></td> <td><span data-ttu-id="694d9-730">単一のセルが選択されている場合</span><span class="sxs-lookup"><span data-stu-id="694d9-730">If a single cell is selected</span></span></td> <td><span data-ttu-id="694d9-731">が`data`文字列の場合、指定したテキストが現在のセルの値として挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-731">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-732">が`data`配列の配列 ("matrix") の場合は、指定された行と列のセットが挿入されます (周囲のセルに含まれるデータが上書きされる場合は除く)。</span><span class="sxs-lookup"><span data-stu-id="694d9-732">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-733">が`data` TableData オブジェクトの場合、指定された行とヘッダーを持つ新しい Excel テーブルが挿入されます (周囲のセルに含まれるデータが上書きされる場合は除く)。</span><span class="sxs-lookup"><span data-stu-id="694d9-733">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-734">複数のセルが選択されている場合</span><span class="sxs-lookup"><span data-stu-id="694d9-734">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="694d9-735">、エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-735">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="694d9-736">、選択されているセルの値が更新されます。`data`</span><span class="sxs-lookup"><span data-stu-id="694d9-736">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="694d9-737">.</span><span class="sxs-lookup"><span data-stu-id="694d9-737"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-738">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-738">Insert images</span></span></td> <td><span data-ttu-id="694d9-739">挿入された画像は浮動になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-739">Inserted images are floating.</span></span> <span data-ttu-id="694d9-740">位置パラメーターの imageLeft と imageTop は、現在選択されているセルからの相対位置になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-740">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="694d9-741">imageLeft と imageTop は負の値にすることもでき、その場合は、画像がワークシート内に収まるようにするために Excel によって再調整される可能性があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-741">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="694d9-742">画像の縦横比は、 imageWidth と imageHeight パラメーターの両方が指定されない限り固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-742">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="694d9-743">imageWidth パラメーターと imageHeight パラメーターのいずれか一方が指定されている場合、もう一方の値は、元の縦横比を維持するように自動調整されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-743">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="694d9-744">その他すべてのケース</span><span class="sxs-lookup"><span data-stu-id="694d9-744">All other cases</span></span></td> <td><span data-ttu-id="694d9-745">エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-745">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-746">Excel Online</span><span class="sxs-lookup"><span data-stu-id="694d9-746">Excel Online</span></span></td> <td><span data-ttu-id="694d9-747">上記の excel で説明されている動作に加えて、これらの制限は excel Online でのデータの書き込み時に適用されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-747">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel Online</span></span></td> <td><span data-ttu-id="694d9-748">この`data`パラメーターを使用してワークシートに書き込むセルの合計数は、このメソッドに対する単一の呼び出しでは2万を超えることはできません。</span><span class="sxs-lookup"><span data-stu-id="694d9-748">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="694d9-749">`cellFormat`パラメーターに渡される書式設定グループの数が100を超えることはできません。</span><span class="sxs-lookup"><span data-stu-id="694d9-749">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="694d9-750">1 つの書式設定グループは、指定のセル範囲に適用される書式設定のセットから構成されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-750">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="694d9-751">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="694d9-751">PowerPoint</span></span></td> <td><span data-ttu-id="694d9-752">画像の挿入</span><span class="sxs-lookup"><span data-stu-id="694d9-752">Insert image</span></span></td> <td><span data-ttu-id="694d9-753">挿入された画像は浮動になります。</span><span class="sxs-lookup"><span data-stu-id="694d9-753">Inserted images are floating.</span></span> <span data-ttu-id="694d9-754">position imageLeft および imagetop パラメーターは省略可能ですが、指定した場合は両方とも存在する必要があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-754">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="694d9-755">1 つの値しか指定されない場合、それは無視されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-755">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="694d9-756">imageLeft と imageTop は負の値にすることもでき、その場合は、画像がスライドの外に配置される可能性があります。</span><span class="sxs-lookup"><span data-stu-id="694d9-756">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="694d9-757">オプションのパラメーターが指定されず、スライドにプレースホルダがある場合は、画像によってスライドのプレースホルダが置き換えられます。</span><span class="sxs-lookup"><span data-stu-id="694d9-757">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="694d9-758">画像の縦横比は、 imageWidth パラメーターと imageHeight パラメーターの両方が指定されない限り固定されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-758">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="694d9-759">imageWidth パラメーターと imageHeight パラメーターのいずれか一方が指定されている場合、もう一方の値は、元の縦横比を維持するように自動調整されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-759">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="694d9-760">[CoercionType](xref:office.Office.CoercionType)パラメーターに指定できる値は、ホストによって異なります。</span><span class="sxs-lookup"><span data-stu-id="694d9-760">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="694d9-761">ホスト</span><span class="sxs-lookup"><span data-stu-id="694d9-761">Host</span></span></th> <th><span data-ttu-id="694d9-762">サポートされる coercionType</span><span class="sxs-lookup"><span data-stu-id="694d9-762">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="694d9-763">Excel、PowerPoint、Project、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-763">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="694d9-764">`Office.CoercionType.Text`表す</span><span class="sxs-lookup"><span data-stu-id="694d9-764">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-765">Excel および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-765">Excel and Word</span></span></td> <td><span data-ttu-id="694d9-766">`Office.CoercionType.Matrix`(配列の配列)</span><span class="sxs-lookup"><span data-stu-id="694d9-766">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-767">Access、Excel、Word</span><span class="sxs-lookup"><span data-stu-id="694d9-767">Access, Excel, and Word</span></span></td> <td><span data-ttu-id="694d9-768">`Office.CoercionType.Table`(TableData オブジェクト)</span><span class="sxs-lookup"><span data-stu-id="694d9-768">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-769">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-769">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="694d9-770">Word</span><span class="sxs-lookup"><span data-stu-id="694d9-770">Word</span></span></td> <td><span data-ttu-id="694d9-771">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="694d9-771">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="694d9-772">PowerPoint、PowerPoint Online</span><span class="sxs-lookup"><span data-stu-id="694d9-772">PowerPoint and PowerPoint Online</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="694d9-773">Excel、PowerPoint、および Word</span><span class="sxs-lookup"><span data-stu-id="694d9-773">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="694d9-774">設定するデータを指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-774">The data to be set.</span></span> <span data-ttu-id="694d9-775">文字列または[CoercionType](xref:office.Office.CoercionType)の値、2d 配列、TableData オブジェクトのいずれかを指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-775">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="694d9-776">渡された値が`data`である場合は、次のようになります。</span><span class="sxs-lookup"><span data-stu-id="694d9-776">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="694d9-777">文字列: プレーン テキスト、または string に強制的に変換できるその他の値が挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-777">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="694d9-778">Excel では、選択したセルに数式を追加する有効な数式としてデータを指定することもできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-778">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="694d9-779">例えば、data を "=SUM(A1:A5)" と設定すると、指定の範囲内の値が集計されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-779">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="694d9-780">ただし、バインドされたセルで数式を設定する場合、その後、バインドされたセルからは追加された数式 (または既存の数式) を読み取ることができません。</span><span class="sxs-lookup"><span data-stu-id="694d9-780">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="694d9-781">選択したセルで Document.getSelectedDataAsync メソッドを呼び出してそのデータを読み取ると、このメソッドは (数式の結果である) セルに表示されたデータのみを返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-781">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="694d9-782">配列の配列 ("matrix"): ヘッダーなしの表形式データが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-782">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="694d9-783">たとえば、2つの列に3つの行のデータを書き込むには、次のよう\[ \[な配列を渡します。 "\]R1C1 \["、"R1C2"、"\]R2C1 \["、"R2C2"、"\]\]R3C1"、"R3C2"。</span><span class="sxs-lookup"><span data-stu-id="694d9-783">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="694d9-784">3つの行からなる1つの列を記述するには\[ \[、次の\]よう\[な配列を\]渡し\[ます。 "R1C1"、"R2C1"、"R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="694d9-784">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="694d9-785">Excel では、データを、選択したセルに追加する有効な数式を含む配列の配列として指定することもできます。</span><span class="sxs-lookup"><span data-stu-id="694d9-785">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="694d9-786">たとえば、他のデータが上書きされない場合は、 \[ \[データを "= SUM (a1: a5)", "= AVERAGE (a1: a5\] \] )" に設定すると、選択範囲に2つの数式が追加されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-786">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="694d9-787">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span><span class="sxs-lookup"><span data-stu-id="694d9-787">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="694d9-788">TableData オブジェクト: ヘッダー付きのテーブルが挿入されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-788">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="694d9-789">excel では、data パラメーターに渡す TableData オブジェクトに数式を指定すると、excel の "集計列" 機能によって期待した結果が得られない場合があります。これにより、列内の数式は自動的に複製されます。</span><span class="sxs-lookup"><span data-stu-id="694d9-789">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="694d9-790">この問題を回避するには、選択`data`したテーブルに数式が含まれている場合は、TableData オブジェクトではなく配列の配列としてデータを指定し、coercionType または "matrix" として指定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-790">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="694d9-791">ただし、この手法では、次のいずれかの条件が満たされている場合にのみ、"計算列" 機能がブロックされます。 (1) 列のすべてのセルに対して、または (2) 列に2つ以上の数式が既に存在します。</span><span class="sxs-lookup"><span data-stu-id="694d9-791">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: callback
          description: '<span data-ttu-id="694d9-792">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-792">Optional.</span></span> <span data-ttu-id="694d9-793">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-793">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-794">.</span><span class="sxs-lookup"><span data-stu-id="694d9-794"></span></span> <span data-ttu-id="694d9-795">取得するオブジェクトまたはデータがないため、AsyncResult プロパティは常に undefined を返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-795">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync
    summary: |-
      <span data-ttu-id="694d9-796">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-796">Project documents only.</span></span> <span data-ttu-id="694d9-797">指定したタスク Id のタスクフィールドを設定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-797">Set task field for specified task Id.</span></span>

      <span data-ttu-id="694d9-798">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-798">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-799">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-799">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-800">タスクフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-800">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="694d9-801">ターゲットフィールドの値。</span><span class="sxs-lookup"><span data-stu-id="694d9-801">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="694d9-802">コールバックで使用するために、変更されていない任意の型のコンテキストデータを保持するためのオプションを提供します。</span><span class="sxs-lookup"><span data-stu-id="694d9-802">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="694d9-803">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-803">Optional.</span></span> <span data-ttu-id="694d9-804">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-804">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-805">.</span><span class="sxs-lookup"><span data-stu-id="694d9-805"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync_1
    summary: |-
      <span data-ttu-id="694d9-806">プロジェクトドキュメントのみ。</span><span class="sxs-lookup"><span data-stu-id="694d9-806">Project documents only.</span></span> <span data-ttu-id="694d9-807">指定したタスク Id のタスクフィールドを設定します。</span><span class="sxs-lookup"><span data-stu-id="694d9-807">Set task field for specified task Id.</span></span>

      <span data-ttu-id="694d9-808">**重要**: この API は、Windows デスクトップの Project 2016 でのみ動作します。</span><span class="sxs-lookup"><span data-stu-id="694d9-808">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-809">例</span><span class="sxs-lookup"><span data-stu-id="694d9-809">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="694d9-810">タスク Id の文字列または値。</span><span class="sxs-lookup"><span data-stu-id="694d9-810">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="694d9-811">タスクフィールド</span><span class="sxs-lookup"><span data-stu-id="694d9-811">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="694d9-812">ターゲットフィールドの値。</span><span class="sxs-lookup"><span data-stu-id="694d9-812">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="694d9-813">省略可能です。</span><span class="sxs-lookup"><span data-stu-id="694d9-813">Optional.</span></span> <span data-ttu-id="694d9-814">コールバックが戻るときに呼び出される関数で、唯一のパラメーターの型が[Office AsyncResult](xref:office.Office.AsyncResult)である。</span><span class="sxs-lookup"><span data-stu-id="694d9-814">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="694d9-815">.</span><span class="sxs-lookup"><span data-stu-id="694d9-815"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.bindings
    summary: <span data-ttu-id="694d9-816">ドキュメントに定義されているバインドへのアクセスを提供するオブジェクトを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-816">Gets an object that provides access to the bindings defined in the document.</span></span>
    remarks: |-
      <span data-ttu-id="694d9-817">Document オブジェクトをスクリプトで直接インスタンス化することはありません。</span><span class="sxs-lookup"><span data-stu-id="694d9-817">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="694d9-818">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span><span class="sxs-lookup"><span data-stu-id="694d9-818">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="694d9-819">例</span><span class="sxs-lookup"><span data-stu-id="694d9-819">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - office.Office.Bindings
  - uid: office.Office.Document.customXmlParts
    summary: <span data-ttu-id="694d9-820">ドキュメント内のカスタム XML パーツを表すオブジェクトを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-820">Gets an object that represents the custom XML parts in the document.</span></span>
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - office.Office.CustomXmlParts
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-821">例</span><span class="sxs-lookup"><span data-stu-id="694d9-821">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.Document.mode
    summary: <span data-ttu-id="694d9-822">ドキュメントのモードを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-822">Gets the mode the document is in.</span></span>
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - office.Office.DocumentMode
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-823">例</span><span class="sxs-lookup"><span data-stu-id="694d9-823">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: office.Office.Document.settings
    summary: <span data-ttu-id="694d9-824">現在のドキュメントのコンテンツ アプリまたは作業ウィンドウ アプリの保存されているカスタム設定を表すオブジェクトを取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-824">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - office.Office.Settings
  - uid: office.Office.Document.url
    summary: <span data-ttu-id="694d9-825">ホスト アプリケーションが現在開いているドキュメントの URL を取得します。</span><span class="sxs-lookup"><span data-stu-id="694d9-825">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="694d9-826">URL が利用できない場合は null を返します。</span><span class="sxs-lookup"><span data-stu-id="694d9-826">Returns null if the URL is unavailable.</span></span>
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="694d9-827">例</span><span class="sxs-lookup"><span data-stu-id="694d9-827">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```